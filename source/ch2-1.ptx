<?xml version="1.0" encoding="UTF-8"?>

<section xml:id="ch2-1" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>matlab/octave</title>

  <p> here are sage cells using matlab/octave syntax. they are as if in a notebook</p>

  <sage language="octave">
    <input>
      % define three vectors
      u = [1 2 0]';
      v = [2 0 3]';
      w = [0 3 4]';

      % construct matrix whose columns are u,v, and w
      A = [u v w];
    </input>
    <output>
    </output>
  </sage>

  <sage language="octave">
    <input>
      % calculate the (signed) volume of the parallelopiped formed by u,v, and w
      
      % first with the determinant of A,
      d = det(A)

      % next with the scalar triple product.
      b = sum(cross(u,v).*w)

      % verify these are equal
      d - b
    </input>
    <output>
    </output>
  </sage>

  <sage language="octave">
    <input>
      
    </input>
    <output>
      what?
    </output>
  </sage>

  </section>
